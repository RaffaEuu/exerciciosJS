function enviar() {
    var recenome = document.preencha.nome.value
    var receidade = document.preencha.idade.value
    var rececep = document.preencha.cep.value
    var recenick = document.preencha.nick.value
    alert("bem vindo " + recenome + receidade + rececep + recenick)
    
}